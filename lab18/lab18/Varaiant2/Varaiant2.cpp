#include <iostream>
#include <cstdarg> // Для работы с переменным числом параметров

// Функция fmin: возвращает минимальное значение среди переданных параметров
int fmin(int n, ...) {
    va_list args;        // Объявление списка аргументов
    va_start(args, n);   // Инициализация списка аргументов с указанием количества параметров

    int minVal = va_arg(args, int); // Извлечение первого аргумента как минимального
    for (int i = 1; i < n; ++i) {
        int current = va_arg(args, int); // Извлечение следующего аргумента
        if (current < minVal) {
            minVal = current; // Обновление минимального значения
        }
    }

    va_end(args); // Завершение работы со списком аргументов
    return minVal; // Возвращаем минимальное значение
}

int main() {
    setlocale(LC_ALL, "Rus");
    // Три различных вызова функции fmin с разным числом аргументов
    int min1 = fmin(4, 10, 20, 5, 15); // 4 аргумента
    int min2 = fmin(3, 7, 2, 9);       // 3 аргумента
    int min3 = fmin(6, 25, 30, 12, 18, 8, 11); // 6 аргументов

    // Вывод минимальных значений
    std::cout << "Минимум (10, 20, 5, 15): " << min1 << std::endl;
    std::cout << "Минимум (7, 2, 9): " << min2 << std::endl;
    std::cout << "Минимум (25, 30, 12, 18, 8, 11): " << min3 << std::endl;

    return 0;
}
